# requirements.txt (修正版)
mss>=6.1.0             # 屏幕捕获
av>=10.0.0             # FFmpeg的Python绑定 (正确的包名是av，不是pyav)
opencv-python>=4.5.5   # 图像处理
pyqt6>=6.2.0           # GUI界面
aioquic>=0.9.20        # QUIC协议实现
numpy>=1.22.0          # 数学计算
pyyaml>=6.0            # 配置文件处理
pytest>=7.0.0          # 测试框架
pygame>=2.1.2          # 可选用于视频渲染
cryptography #生成证书
pyautogui #获取鼠标位置